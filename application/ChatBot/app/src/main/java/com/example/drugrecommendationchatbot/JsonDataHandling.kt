package com.example.drugrecommendationchatbot

import com.google.gson.JsonObject

object JsonDataHandling {

    fun processJsonData(jsonObject : JsonObject){
        //Json object를 받아서, 원하는 데이터 객체로 바꿔줌

        when(jsonObject["type"]){

        }
    }

}